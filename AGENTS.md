# AGENTS.md

## 🧠 项目目标

这是一个 **AstrBot 插件**，用于调用 AI（如智谱AI / OpenAI / 其他模型）生成视频。

你必须按照 **AstrBot 插件规范** 开发，而不是普通 Python 项目。

---

## 📦 插件定位

插件名称：`astrbot_plugin_kongcheng_ai`

功能包括（但不限于）：

- 文本生成视频
- 图片生成视频
- 可扩展：

---

## ⚠️ 必须遵守的开发规则

### 1️⃣ 插件结构规则

- 严格按照AstrBot目录下docs/en/dev/star/plugin-new.md的插件指引规则

---

### 2️⃣ AstrBot 编程规范

- 事件处理必须基于 AstrBot 事件系统

---

### 6️⃣ 文件与资源管理（重要）

- 临时文件必须删除（如下载图片）
- 如果下载视频：
  - 必须提供开关（是否保存本地）
  - 建议保存路径：`data/video_cache/`
- 禁止无限制占用磁盘

---

### 7️⃣ 安全规范

- 不要执行系统命令（subprocess）除非必要
- 不要暴露敏感错误信息给用户

---

## 📚 优先参考顺序（非常重要）

在写代码前必须按顺序阅读：

1. 当前目录下的其他示例插件
2. AstrBot仓库下的 `docs/`
3. AstrBot 插件结构
4. 现有插件实现

---

## 🧩 代码风格要求

- 保持结构清晰（函数不要过长）
- 合理拆分功能（生成 / 查询 / 下载）
- 避免重复代码
- 使用类型标注（typing）

---

## 🚀 拓展功能（可选实现）

- 视频自动下载
- 视频直链返回
- 多模型支持（智谱 / OpenAI / Runway）
- 失败重试机制
- 限流（防止滥用）

---

## 🧠 行为准则（很重要）

- 你是在开发 AstrBot 插件，不是写脚本
- 优先保证“可运行 + 符合框架”
- 不确定时优先参考示例插件，而不是猜

---

## ❌ 禁止行为

- ❌ 写成普通 CLI 程序
- ❌ 忽略 metadata.yaml
- ❌ 使用同步阻塞代码
- ❌ 忽略异常处理
- ❌ 写无法在 AstrBot 中运行的代码

---

## ✅ 最终目标

生成的插件必须：

- 可以直接放入 AstrBot/data/plugins/ 运行
- 支持基本 AI 视频生成能力
- 结构清晰、可扩展
